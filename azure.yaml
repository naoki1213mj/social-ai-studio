# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: social-ai-studio
metadata:
  template: social-ai-studio@0.1.0
services:
  app:
    project: .
    host: containerapp
    language: python
    docker:
      path: ./Dockerfile
      context: .
